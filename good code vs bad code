STATUS_MESSAGES = {
    'active': 'Active',
    'inactive': 'Inactive'
}
def process(d):
    #It Retrieve the status from the dictionary if it  default it displays  'Unknown' if not found
    status = d.get('status', 'unknown')
    print(STATUS_MESSAGES.get(status, 'Unknown'))
